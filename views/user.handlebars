{{> nav}}

<div class="container has-background-white p-md m-lg-bottom" id="budget">
    <h1 class="title">{{section}}</h1>
    <br>

    <div class="flex-center p-md">
        <div class="field has-addons">
            <div class="control">
                <input type="text" id="newEntry" class="input" placeholder="New Entry">
            </div>
            <div class="control">
                <input type="text" id="newAmount" class="input" placeholder="Amount">
            </div>
            <div class="control">
                <span class="select">
                    <select name="category" id="topCategory">
                        <option value="">Select Category</option>
                        {{#each userData}}
                        <option value="{{category}}">{{category}}</option>
                        {{/each}}
                    </select>
                </span>
            </div>
            <div class="control">
                <button class="button is-primary">Add</button>
            </div>
        </div>
    </div>

    {{#each userData}}
        <table class="table is-fullwidth is-striped" id={{category}}>
            {{> category}} 
            
            {{> entries}}
        </table>
            
    {{/each}}
    
</div>

<script type="text/javascript" src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
<script type="text/javascript" src="/js/user.js"></script>

